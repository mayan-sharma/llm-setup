interface:
  display_name: "Implementation Loop"
  short_description: "Plan, critique, finalize, then implement"
  default_prompt: "Use $implementation-loop to plan this goal, critique the plan for minimalism and correctness, finalize it, then implement."
