interface:
  display_name: "Resume Checkpoint"
  short_description: "Verify and resume saved repository work"
  default_prompt: "Use $resume-checkpoint to verify and continue from a saved checkpoint."
